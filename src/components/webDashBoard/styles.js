const styles = theme => ({
  wrapper: {
    display: 'flex',
    flexDirection: 'row',
  },
  wrapperContent: {
    height: '100%',
    width: '100%',
    padding: 5,
    backgroundColor: 'red',
  },
});
export default styles;
